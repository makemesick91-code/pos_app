name: UIX-6 CI

on:
  # CICD-CTRL-2: neutralized to manual-only; superseded by ci-authoritative.yml (Lane B)
  # and ci-main-smoke.yml (Lane C). Full validation still runnable on demand.
  workflow_dispatch:
jobs:
  support-observability-incident-gate:
    name: UIX-6 support/observability/incident console gate + backend regression
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Application foundation rules gate
        run: bash scripts/verify_application_foundation_rules.sh

      - name: UIX-6 support/observability/incident console design gate (chains UIX-5/4/3/2/1)
        run: bash scripts/uix6_design_gate.sh

      - name: UIX-6 rules present (UIX6-R001..R033)
        run: |
          for i in $(seq -w 1 33); do
            grep -q "UIX6-R0$i" docs/PROJECT_RULES.md
            grep -q "UIX6-R0$i" docs/foundation/uix-6-support-observability-incident-console.md
            grep -q "UIX6-R0$i" .claude/rules/45-support-observability-incident-governance.md
          done

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.5"
          extensions: pdo, sqlite3, pdo_sqlite, mbstring, bcmath
          coverage: none

      - name: Install backend dependencies
        working-directory: backend
        run: composer install --prefer-dist --no-progress --no-interaction

      - name: Prepare sqlite environment
        working-directory: backend
        run: |
          cp .env.example .env
          php artisan key:generate
          mkdir -p storage/app storage/logs storage/framework/views storage/framework/cache storage/framework/cache/data storage/framework/sessions storage/framework/testing bootstrap/cache
          touch database/database.sqlite
          php artisan migrate --force || true
        env:
          DB_CONNECTION: sqlite
          DB_DATABASE: database/database.sqlite

      - name: UIX-6 feature tests
        working-directory: backend
        run: php artisan test --testsuite=Feature --filter='Uix6'
        env:
          DB_CONNECTION: sqlite
          DB_DATABASE: database/database.sqlite

      - name: Backend full regression suite
        working-directory: backend
        run: php artisan test
        env:
          DB_CONNECTION: sqlite
          DB_DATABASE: database/database.sqlite
