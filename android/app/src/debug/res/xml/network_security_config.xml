<?xml version="1.0" encoding="utf-8"?>
<!--
  UIX-7 DEBUG-ONLY transport policy (UIX7-R046/R048).

  This file lives in the `debug` source set, so it fully overrides the cleartext-
  denied src/main policy ONLY for the emulator `debug` variant. It permits
  cleartext strictly to the local developer host aliases used by the Android
  Emulator; every other host (including https://aishpos.online) still requires
  validated TLS against the system trust store. There is no trust-all anchor and
  no hostname-validation override.

  The `pilot` and `release` variants do NOT include this source set, so they never
  inherit these local cleartext exceptions.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator alias for the developer's host machine. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
