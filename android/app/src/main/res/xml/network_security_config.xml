<?xml version="1.0" encoding="utf-8"?>
<!--
  UIX-7 transport security (UIX7-R026/R027, rule 30/80 HTTPS posture).

  Cleartext HTTP is DENIED by default, so the pilot backend
  (https://aishpos.online) and every real host must be reached over TLS. The
  previous manifest-wide android:usesCleartextTraffic="true" is removed in favour
  of this scoped policy. Cleartext is permitted ONLY for the local emulator/dev
  host aliases used during development — never for a production/pilot host.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator alias for the developer's host machine. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
