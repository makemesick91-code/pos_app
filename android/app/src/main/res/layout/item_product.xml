<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingVertical="@dimen/space_md"
    android:paddingHorizontal="@dimen/space_xs">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/textName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.Aish.CardTitle"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/textMeta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.Aish.Caption"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/textPrice"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textAppearance="@style/TextAppearance.Aish.Body" />

        <!-- Sprint 8 — lightweight, informational stock label. Defaults to
             "Stok: -" until the backend stock fetch resolves. -->
        <TextView
            android:id="@+id/textStock"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textAppearance="@style/TextAppearance.Aish.Caption"
            android:text="@string/cashier_stock_unknown" />

    </LinearLayout>

    <Button
        android:id="@+id/buttonAdd"
        style="@style/Widget.Aish.Button.Primary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/space_sm"
        android:minHeight="@dimen/touch_target_min"
        android:text="@string/cashier_add" />

</LinearLayout>
